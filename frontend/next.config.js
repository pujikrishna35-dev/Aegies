/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'aegisoverseas.com', 'wordpress.validthemes.net']
  }
};

module.exports = nextConfig;
